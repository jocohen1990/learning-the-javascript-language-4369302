2 + 5; // 7
4 - 3; // 1
5 - 9; // -4
3 * 5; // 15
36 / 6; // 6
36 / 5; // 7.2

20 % 2; // 0. Modulus operator that takes the first operand, divides it by the second, and then returns the remainder of that division. Often used to determine whether a number is even or odd, like in a list of search results. 20/2 = 10, no remainder so the result is 0.
19 % 2; //1. 19/2 is 9.5

// twenty an even number?
20 % 2 === 0; // true

const perPage = 20;
const totalResults = 254;
totalResults % perPage;

var counter = 0;
counter = counter + 1; // 1

counter += 1; // 2
counter++; // 2

counter += 5; // 8
counter += -4; // -4

counter -= 1; // 3 
counter--; // 3
counter; 

counter *= 2; // 4

"cat" + "dog"; //catdog
"cat " + "dog"; // 'cat dog'
"cat" + " and " + "dog"; // 'cat and dog'

"1" + "2"; //string concatenation, results in 12,not 3.

// More info:
// https://developer.mozilla.org/en-US/docs/JavaScript/Guide/Expressions_and_Operators#Arithmetic_operators
// https://developer.mozilla.org/en-US/docs/JavaScript/Guide/Expressions_and_Operators#String_operators
